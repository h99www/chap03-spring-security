# port config
server:
  port: 8001

# datasource config
spring:
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@oracle.team-greedy.com:1521:xe
    username: C##BOOT_AUTH
    password: BOOT_AUTH

# mybatis config
mybatis:
  mapper-locations: mappers/**.xml
